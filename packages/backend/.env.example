# Database (matches docker-compose.yml)
DATABASE_URL="postgresql://ppl_user:ppl_dev_password@localhost:5432/ppl_app?schema=public"

# JWT
JWT_SECRET="change-this-to-a-real-secret-in-production"
JWT_EXPIRES_IN="7d"

# Stripe (get keys from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Twilio (get from https://console.twilio.com)
TWILIO_ACCOUNT_SID="AC..."
TWILIO_AUTH_TOKEN="..."
TWILIO_PHONE_NUMBER="+1..."

# Email (Gmail SMTP or any SMTP provider)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="\"Pitching Performance Lab\" <noreply@pitchingperformancelab.com>"

# App
PORT=4000
NODE_ENV="development"
FRONTEND_URL="http://localhost:3000"
