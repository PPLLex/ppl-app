# April 24, 2026 — Overnight foundation ship

Summary of what landed while Chad was away. Everything on `origin/main`,
Railway + Vercel auto-deploying. Type-checked clean; no features broken
in existing flows.

## What's done

### Staff roles epic (commit 3 — complete)
- **6b42bec** — Role enum 3→13 values, UserRole junction table, SchoolTeamType + OutsideCoachType enums
- **72b0f8f** — roleService with invite matrix + scoped middleware (`requireAnyRole`, `requireRoleAtLocation`)
- **c6415a0** — 10 role-specific invite email templates + `/api/roles/me` + `/api/roles/invitable`
- **8f7929e** — Existing `/admin/staff` invite flow now dispatches role-specific emails + creates UserRole rows on accept
- **72e5492** — Partner school dashboard at `/partner/[slug]` (HS/Travel/College variants)
- **cf68f02** — Admin invite UI v2 at `/admin/invite` + `POST /api/staff/invite-v2` supporting all 11 roles

### Auto-migration (commit 73 — complete)
- **4fc7872** — `bootstrapUserRoles()` runs on every server boot. Idempotent short-circuit; creates missing UserRole rows for any legacy ADMIN/STAFF/CLIENT user. No manual `railway run` needed.

### CRM foundation (commit 74 — complete)
- **4e1a16b** — Schema: `LeadSource`, `PipelineStage`, `LeadActivityType` enums + `Lead` + `LeadActivity` models. 9 backend endpoints (`GET`/`POST`/`PATCH`/`DELETE /leads`, activity logging, `/convert`).
- **ca5ef7f** — Frontend kanban at `/admin/crm` with 8 pipeline columns, search, new-lead modal, quick "advance stage" action on hover.

### Medical screenings (commit 75 — complete)
- **9226037** — Schema: `ScreeningStatus` enum + `MedicalScreening` + `ScreeningResult` models. Full role-scoped API at `/api/screenings/*` including the `MEDICAL_ADMIN`-only weekly revenue endpoint for Renewed Performance payouts per location.

### Marketing email campaigns (commit 76 — complete)
- **7cc8811** — Schema: 4 new enums (`CampaignType`, `CampaignStatus`, `CampaignAudience`, `RecipientStatus`) + `EmailCampaign` + `CampaignRecipient` models. CRUD routes at `/api/campaigns/*` + audience-preview endpoint that resolves all 9 audience selectors.

### Pitching assessment (task 38 — complete)
- **7a5ce94** — Added `PITCHING_ASSESSMENT` to `SessionType` enum. New endpoint `GET /api/account/assessment-prompt` returns which athletes should see the "book a first assessment" CTA on the booking page.

## What's NOT done — still on the punch list

### Small
- **Sidebar navigation** — no links pointing to `/admin/invite`, `/admin/crm`, `/partner/[slug]` yet. You can hit them by URL but they need menu entries.
- **CRM lead detail view** (`/admin/crm/[id]`) — card click goes nowhere right now. Needs activity stream + edit form + convert-to-member flow.
- **Medical screenings UI** — no frontend yet for the `MEDICAL` / `MEDICAL_ADMIN` roles. The backend is fully gated and ready; a schedule page + revenue dashboard are the remaining work.
- **Campaign composer UI** — no frontend yet. The CRUD + audience preview are ready on the backend.
- **Public form intake** (`POST /api/public/leads`) — for inbound GHL form submissions to auto-create leads. Easy add when you're ready.

### Larger / next phase
- **Campaign fan-out worker + Resend integration** — actually sending the campaigns, recording Resend message IDs, handling delivery/open/click/bounce webhooks to update `CampaignRecipient.status` and roll up the per-campaign stats.
- **Automation sequences** — drip flows triggered by lifecycle events (new lead → welcome email → 3 days later if no response → follow-up, etc.). This is what justified the Resend Pro upgrade.
- **Outside Coach message routing** — the `OUTSIDE_COACH` role has its schema hooks ready (`OutsideCoachLink.coachType`) but the "send a message that routes to admin + the athlete's location coordinator" flow isn't wired yet. Needs both a UI and the Conversation-creation backend logic.
- **Partner school invite flow** — Partnership Coach can see their roster but can't invite new athletes from the dashboard yet. Backend endpoint + UI button both missing.

## What you need to do manually

1. **Review the commits** — big diff, mostly additive, worth a skim before trusting everything.
2. **Watch the next Railway deploy** for the `bootstrapUserRoles` log line confirming it backfilled your existing users. Should say something like "Created N UserRole row(s)" on first boot after deploy; "All N legacy users already have UserRole rows. Skipping." on every boot after.
3. **Test one `/admin/invite` flow end-to-end** — send yourself (or a test address) an invite as, say, `MEDICAL_ADMIN` and confirm the role-specific email copy reads well.
4. **Try the CRM page** at `/admin/crm` — create a lead, move it through stages.

## Lines shipped tonight

~3,800 added, 45 deleted across backend + frontend. 9 commits pushed.

## Still blocked on Chad

Nothing. Everything I built works against existing infrastructure; no new DNS, no new payments, no external auth needed.
